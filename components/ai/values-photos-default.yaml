---
ingress:
  main:
    enabled: true
    hosts:
      - host: photos.kassio
        paths:
          - path: /
persistence:
  config:
    storageClass: local-path
    size: 5Gi
  originals:
    storageClass: local-path
    size: 5Gi
