bases:  
  - ../bases/repos.yaml
  - ../bases/environments.yaml

{{ readFile "../bases/templates.yaml" }}

commonLabels:
  component: zigbee

releases:
  - name: kassio-zigbee
    <<: *release
    chart: zigbee2mqtt/zigbee2mqtt
    version: 2.2.1