bases:  
  - ../bases/repos.yaml
  - ../bases/environments.yaml

{{ readFile "../bases/templates.yaml" }}

commonLabels:
  component: logs

releases:
  - name: kassio-logs
    <<: *release
    chart: grafana/loki-stack
    version: 2.5.0