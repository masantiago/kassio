bases:  
  - ../bases/repos.yaml
  - ../bases/environments.yaml

{{ readFile "../bases/templates.yaml" }}

commonLabels:
  component: workflow

releases:
  - name: kassio-workflow
    <<: *release
    chart: 8gears/n8n
    version: 1.0.0